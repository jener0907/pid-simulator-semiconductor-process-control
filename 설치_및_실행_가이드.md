# 온도 제어 시뮬레이터 설치 및 실행

## 준비

- Windows 컴퓨터
- Python 설치
- 첫 실행 시 패키지 설치를 위한 인터넷 연결

Python 설치 후 명령 프롬프트에서 `python --version`을 입력해 버전이 표시되는지 확인하세요. 명령을 찾을 수 없다고 나오면 Python 설치 프로그램을 다시 실행해 **Add Python to PATH**를 선택하세요.

## 실행

1. ZIP 파일을 원하는 폴더에 압축 해제합니다.
2. 압축을 푼 폴더에서 `run.bat`을 더블클릭합니다.
3. 첫 실행 때 `.venv` 가상환경을 만들고 필요한 패키지를 설치합니다. 설치가 끝나면 시뮬레이터가 시작됩니다.
4. 브라우저가 자동으로 열리지 않으면 주소창에 `http://127.0.0.1:8501`을 입력합니다.

실행 중에는 검은 명령 프롬프트 창을 닫지 마세요. 종료하려면 그 창에서 `Ctrl+C`를 누르거나 창을 닫으면 됩니다.

## 사용법

- 왼쪽 설정에서 설정 온도, 공정 조건, 제어기 값을 수정합니다. 값 변경 시 결과가 다시 계산됩니다.
- `그래프 화면` 탭에서 온오프 전체 곡선, 40~70분 확대 그래프, 세 제어기 비교 그래프를 확인합니다.
- `수치표` 탭에서 시간별 온도와 히터 출력 값을 확인하고, `CSV로 결과 저장`을 눌러 내려받습니다.
- 왼쪽 `그래프 표시` 체크박스로 그래프의 선을 켜고 끌 수 있습니다.

## 직접 실행하기

`run.bat` 대신 폴더에서 명령 프롬프트를 열어 아래 명령을 차례로 실행할 수도 있습니다.

```bat
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
.venv\Scripts\python.exe -m streamlit run app.py --server.address 127.0.0.1
```

이 안내서는 로컬 실행 방법입니다. 웹 링크로 공유하려면 GitHub 저장소를 Streamlit Community Cloud에 연결하세요. 자세한 단계는 `README.md`를 참고하세요.
